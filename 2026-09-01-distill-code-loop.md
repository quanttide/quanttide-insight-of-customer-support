# Distill 逻辑：CODE 循环四步

> 展开 2026-09-01 设计洞察之 distill 篇。用 CODE 循环（Capture → Organize → Distill → Express，Tiago Forte《Building a Second Brain》）梳理支持云蒸馏管线的四个步骤。与专篇《2026-09-01-background-knowledge.md》（背景类知识）、《2026-09-01-design-insights.md》（综合洞察）互参。

## CODE 映射总览

| CODE | 支持云对应 | 管线阶段 | 资产层 |
|---|---|---|---|
| **C**apture 捕获 | 抓取支持活动原始流 | import | journal |
| **O**rganize 组织 | 案例化整理为五要素记录 | import 的加工 | journal（结构化） |
| **D**istill 提炼 | 从实例蒸馏为规则条目 | distill | profile |
| **E**xpress 表达 | 检索应答 + 反哺知识体系 | serve | 应用 + 全域 |

关键认识：CODE 在第二大脑里是个人笔记方法论，支持云把它变成**多智能体生产管线**——C/O 由 import 自动完成，D 是人机协作的蒸馏车间，E 同时服务客户（应答）和公司（信号回流）。

## 第一步：Capture——把对话流变成可处理的原料

**做什么**：拉取全部支持触点的原始记录（群聊、邮件、工单、一对一），不筛选、不加工，全量落档。

今天的实证：lark-cli 分页拉取 6 天 388 条群消息，系统消息过滤、发送者真名提取、单页落盘再解析。

设计要点：

- **全量是纪律**：捕获阶段的唯一任务是保真——筛选是后面的事，这里丢一条，蒸馏就少一块原料。原始交互档案"永不丢弃"原则的执行点
- **多源归一**：群聊/邮件/工单格式各异，capture 要把它们统一成同一种消息模型（时间/人/内容/类型）
- **来源即元数据**：每条消息自带时间戳、频道、发送者——后面 O 和 D 全靠这些定位

## 第二步：Organize——从消息流到案例流

**做什么**：把消息按"一次支持事件"聚类，整理成五要素案例：**客户 → 问题 → 解答 → 状态 → 沉淀**。

今天的实证：388 条消息 → 8 天支持 journal，每天按议题分组、每案例五要素，关键原话保留。

设计要点：

- **聚类单元是"事件"不是"天"**：一次跨两小时、双人接力的解答是一个案例，不是两条
- **五要素是 O 与 D 的契约**：客户/问题/解答/状态/沉淀，每个字段都是下游的输入——「状态：已解决」是蒸馏触发旗标，「沉淀」是条目种子，「来源」让复用率可数。import 的格式设计直接决定 distill 能否自动化，耦合点在这里
- **系统消息、寒暄在此过滤**：capture 保真，organize 才做判断——两步分开，各自简单

## 第三步：Distill——从实例到类（核心车间）

**做什么**：把已解决的案例蒸馏成解决方案条目：去个性化（人名→元数据）、泛化（实例→规则）、补边界（加升级判据），四段式条目（适用场景/解答/升级判据/来源与复用）入库并登记索引。

### 触发：distill 内部的批流一体

| 触发 | 机制 | 保什么 |
|---|---|---|
| 流触发 | 案例状态=已解决 → AI 起草候选 → 回答者确认入库 | 时效（FAQ 当天） |
| 批触发 | 每周清扫 journal：已解决但查无条目的案例 | 完整（兜底防漏） |
| 频率信号 | 同类问题 2+ 次无条目 → 蒸馏提级 | 优先级 |

三种触发的输入全部埋在五要素格式里，不需要新数据。

### 蒸馏分两档（详见背景类专篇）

- **自动档**（FAQ/制度/操作指引）：AI 起草 → 回答者抽查确认
- **人工档**（背景/演进）：AI 备料（时间线线索+消息聚类）→ 人拼因果链 → 确认署名。错误的背景比没有背景更毒，这 10 分钟不能省

### 质量门

1. **升级判据强制显式化**：每条生效条目必须写判据，哪怕内容是「无——AI 可自由应答」。空字段语义模糊（没写≠没有），蒸馏时刻是唯一能问到知识边界的时机
2. **入库即索引**：条目 merge 必须同步登记 README 索引——只入库不进索引等于没入库（解决方案可见性，delib「决议可见性」的同构痛点）

### 状态机

```
journal案例 → 候选条目(AI起草) → 生效条目(已确认+已索引) → 归档条目(失效/被替代)
```

与 delib 同构：案例=议题、候选=决议草案、生效=决议、归档=决议归档。复用记录=决议的执行跟踪。失效必须显式标记而非原地改——保留演进史，背景类知识就是这样攒出来的（减法优先=失效归档，不是删除）。

### 执行形态：PR-based 蒸馏队列

候选条目 = 对 profile 仓库的 PR（正文附来源链接与入库理由）→ 回答者在 PR 里 approve/edit/reject → merge 即生效。审核界面、版本历史、责任人、回滚全部白拿 git 的——今天手工入库走的就是这条路，只是手工。

### 谁来蒸馏

起草者：AI（自动档）/ 支持者本人（人工档）。确认者：**回答那条问题的人**——答案的上下文在他脑子里，确认是一键审核而非重新调研。实训任务化：学员蒸馏案例=强迫读懂业务，同时解决人力池、教学素材、众包标的三个问题——蒸馏从支持成本变成教学内容。

## 第四步：Express——资产服务两端

**做什么**：条目库被消费，产出双向价值：

- **对外（serve 客户）**：AI 检索条目第一级应答，回答附「由条目 X 支持，已复用 N 次」（引用即交付信任）；命中升级判据即转人工。主动拦截：客户提出已被否过的方向时，背景条目自动提示历史结论
- **对内（反哺公司）**：需求与缺陷信号结构化后喂给需求地图——支持是法源的传感器；复用率、无需人参与比例从 journal 基线持续出趋势线

**Express 的输出回流 Capture，循环闭合**：客户的新问题、新的升级案例再次进入原料流——这正是 CODE 循环区别于线性流程的地方：E 不是终点，是下一轮 C 的输入。自进化 = 这个环转得越来越快、C 到 E 的路径上人工越来越少。

## 四步的分工一句话

- **Capture 保真**（全量、多源归一）
- **Organize 建模**（消息流→五要素案例流）
- **Distill 造资产**（实例→类，人机分档，判据显式化）
- **Express 生价值**（应答客户 + 信号回流，输出再入循环）

管线里程碑顺序即 CODE 顺序：先把 C/O 做稳（journal 格式已验证），再做 D 的自动档（FAQ 蒸馏 PR 队列），背景档人工并行；E 的自动应答最后做——库不厚，应答越自动越快地暴露错误。
