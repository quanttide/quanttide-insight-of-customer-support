# 背景类知识：AI 提炼不出的那类资产

> 展开 2026-09-01 设计洞察之三。来源案例：08-30 邮件工作流课题的 1 小时人工深谈（journal/2026-08-30#案例一 → profile/qttech/email-workflow-evolution.md，现 qtclass/）。

## 一、从那次 1 小时深谈说起

08-30 候选人 W 提「想升级邮件工作流」，方向没错，但被否过一次——为什么否？因为有一个**隐藏背景**：这套工作流刚从群聊迁移到邮箱（企业微信接口导不出数据），而邮箱也只是过渡（业务正走向自研平台）。不知道这个演进史，就会给出一个「技术上完全正确、但三个月后就被平台化淘汰」的方案。

这个背景在原始群消息里**存在但不可提炼**：它分散在 6 天的对话里，混杂在闲聊和追问中，没有任何一条消息单独承载它。AI 批量处理原始日志时，这类信息是噪声——单条看都是闲聊，连起来才是知识。

那次是人工跨 6 天拼出时间线、再由导师确认每个转折的原因，才固化成 `email-workflow-evolution.md`。**1 小时深谈换一条条目——这个成本就是「老员工的经验」的成本**，以前付了就蒸发（答案死在对话里，下一个问同样问题的人重新触发深谈），现在付一次就永久入库。

## 二、背景类知识改变 AI 回答的「质地」

同样的客户问题「我想做邮件自动化」：

| | 只有 FAQ 的 AI | 有背景条目的 AI |
|---|---|---|
| 回答 | 「可以，我们的工作流是 E1-E18，文档在这里」 | 「邮件自动化我们探索过，难点不在写邮件而在判断工作状态——批量回复必出错，所以收益更高的路线是自研平台对接。建议从共识链提炼而不是重构工作流」 |
| 质地 | 像说明书，甚至会把已被淘汰的路线当成可行方案推荐 | 像老兵：知道「我们试过什么、为什么放弃、坑在哪」 |
| 风险 | 推荐死路 | 提前拦截 |

区别的本质：FAQ 回答「**是什么**」，背景回答「**为什么是这样、什么路走不通**」。客户对后者的信任感完全不同——这是意图「交付信任」锚点的知识侧基础。

## 三、对批蒸馏管线的设计含义：蒸馏分两档

蒸馏不是匀质的，这是本条最可执行的结论：

| 档位 | 知识类型 | 提炼方式 | 人工角色 |
|---|---|---|---|
| 自动档 | FAQ / 制度 / 操作指引 | AI 从 journal 批量抽取候选 → 结构化成条目 | 抽查审核 |
| 人工档 | **背景/演进** | AI 提供时间线线索 + 相关消息聚类，**拼图必须人完成** | 深谈 + 确认 + 署名 |

为什么人工档不能全自动：背景的载体是「转折和原因」，而转折在消息流里没有标记（「这个想法被否过」不会自带「因为」）。AI 能做的是把**相关消息聚到一起**（按实体、时间、话题漂移），把拼图原料备好——人只做最后一步「确认因果链」。

这已经把 1 小时压缩到可能 10 分钟，但那 10 分钟不能省：**错误的背景比没有背景更毒**——AI 会自信地传播一个错的时间线。

## 四、副产品：背景条目是「防重复踩坑」清单

成型的背景条目自带一组「我们试过/放弃了什么」。二次用途：**当客户或学员提出一个已被否过的方向时，系统主动提示**——「这个方向 8 月底讨论过，否掉的原因是 X，建议看 Y」。

这把支持从「被动应答」升级成「主动拦截」，挡掉的是导师每周要花几小时重复纠正的话。知识的复用价值在这里最高。

## 收束

FAQ 让 AI 能答，背景让 AI 答得像自己人。支持云自进化的上限取决于背景档的沉淀速度——**管线第一个里程碑应该是「背景提取辅助」（distill 阶段），而不是更花哨的自动应答（serve 阶段）**。
